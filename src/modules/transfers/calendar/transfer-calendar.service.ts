import { TransferCalendarQuery } from './transfer-calendar.types';

export async function getTransferCalendarService({
  month,
  scheduleId,
}: TransferCalendarQuery) {
  // Implementation for fetching transfer calendar availability
}
